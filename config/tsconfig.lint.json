{
  "compilerOptions": {
    "module": "esnext", // needed???
    "noEmit": true, // Don't build if we're linting
    "target": "esnext" // needed???
  },
  "exclude": [], // Don't exlude __tests__ so we can lint them
  "extends": "./tsconfig.base.json"
}
